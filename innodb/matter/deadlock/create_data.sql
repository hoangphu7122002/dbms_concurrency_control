USE dbms_assignment;

CREATE TABLE table1 (
	id INT(16) NOT NULL AUTO_INCREMENT,
	name VARCHAR(10) NOT NULL,
	value int(11) NOT NULL,
    primary key (id)
);

CREATE TABLE table2 (
	id INT(16) NOT NULL AUTO_INCREMENT,
	name VARCHAR(10) NOT NULL,
	value int(11) NOT NULL,
    primary key (id)
);